{
  "name": "quicksight-custom-parameter-display",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp src/index.html dist/",
    "start": "npm run build && concurrently \"npx tsc --watch\" \"live-server dist --entry-file=index.html --port=8080\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
